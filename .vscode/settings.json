// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/.git": true,
        "**/node_modules": true,
        "**/*.js.map": { "when": "$(basename)"},
        "**/*.js": { "when": "$(basename).ts"}
    }
}